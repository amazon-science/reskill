torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
transformers==5.5.0
ray==2.48.0
click==8.1.8
vllm==0.8.5
hydra-core==1.3.2
omegaconf==2.3.0
numpy==1.26.4
pandas==2.3.3
pyarrow==24.0.0
gymnasium==1.3.0
wandb==0.25.1
requests==2.34.2

# Search-specific additions
# faiss-gpu must be installed from a pre-downloaded wheel (not from PyPI/conda)
# because the internal mirror does not carry it. The validated wheel is:
#   faiss-gpu==1.7.2  (cp310, manylinux2014_x86_64, CUDA 12)
# Retrieve the .whl from the staged NFS copy and install offline:
#   pip install faiss_gpu-1.7.2-cp310-cp310-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
faiss-gpu==1.7.2

# Retrieval server (FastAPI + Uvicorn)
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4

# Misc used by retrieval pipeline
tqdm==4.67.3
httpx==0.28.1
aiohttp==3.13.5

# flash_attn: install from prebuilt CUDA 12 / Torch 2.6 wheel — not a universal
# package constraint. See constraints-alfworld-vllm-cu124.txt for guidance.
